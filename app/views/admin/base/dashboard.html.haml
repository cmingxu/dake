%h2
  - if current_user.caiwu_admin?
    - current_user.locations.each do |location|
      %br
      您当前负责
      = link_to location.name, edit_admin_location_path(location)
      地区, 本地区客车有
      - location.routes.each do |r|
        = link_to r.friendly_name, edit_admin_route_path(r)
%hr

- if current_user.caiwu_admin?
  = link_to " 支出 ", new_admin_balance_path, :class => "btn btn-large btn-primary"
  = link_to " 收入 ", new_admin_in_balance_path, :class => "btn btn-large btn-primary"

%hr
- if current_user.huozhan_admin?
  = link_to " 收件 ", new_admin_ship_out_path, :class => "btn btn-large btn-primary"
  = link_to " 发货 ", new_admin_cargo_out_path, :class => "btn btn-large btn-primary"
  = link_to " 收货 ", admin_cargo_ins_path, :class => "btn btn-large btn-primary"
  = link_to " 放件 ", admin_ship_ins_path, :class => "btn btn-large btn-primary"
